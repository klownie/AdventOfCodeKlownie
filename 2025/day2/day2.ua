Parse ← (
  ⊜□⊸≠@,
  ≡(□⍜-⇡⊓(∘|+₁)°⊟⊜⋕⊸≠@-°□)
  ≡(□°⋕)/(⊂∩(°□))
)
Part₁ ← (
  ⊏⊚⊸≡(≍⍣(⊃(↘|↘⟜↻)|0)÷₂⊸⧻°□)
  /+≡(⋕°□)
)
Part₂ ← (
  ⊏⊚⊸≡(
    °□
    ⇡⊃(⧻|¤)
    ≡(⦷⊸↙)
    >₀/+=₀/+⍉=₀
  )
  /+≡(⋕°□)
)

&fras$ input
